shop_homepage:
    path:     /
    defaults: { _controller: ShopBundle:Default:index }
category:
    path: /category/{slug}
    defaults: { _controller: ShopBundle:Category:index }
products:
    path: /products/{slug}
    defaults: { _controller: ShopBundle:Products:index }
buyit:
    path: /buyit
    defaults: { _controller: ShopBundle:Payment:prepare }
done:
    path: /paymentdone
    defaults: { _controller: ShopBundle:Payment:done }
orders:
    path: /products/{slug}/order
    defaults: { _controller: ShopBundle:Orders:new }